export const resourceObjectTypes = Object.freeze({
    ROCK: 'rock',
    TREE: 'tree',
    MINE: 'mine'
});

export const problemTypes = Object.freeze({
    BTREE: 'btree',
    IMAGE: 'image',
    REGEX: 'regex'
});

export const resourceTypes = Object.freeze({
    STONE: 'stone',
    WOOD: 'wood'
});
