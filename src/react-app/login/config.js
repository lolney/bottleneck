const config = {
    issuer: 'https://dev-169479.oktapreview.com/oauth2/default',
    redirect_uri: window.location.origin + '/implicit/callback',
    client_id: '0oaigopumvEKEibQR0h7',
    scope: 'openid profile email'
};

export default config;
